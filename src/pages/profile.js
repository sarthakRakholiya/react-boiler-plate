/**
 * It returns the AppLayout component, which contains the ProfileMain component
 * @returns A function that returns a component
 */
import ProfileMain from '../components/pages/Profile';

const Profile = () => {
  return (
    <>
      <ProfileMain />
    </>
  );
};

export default Profile;
