/**
 * It returns a div with a class of loader-section, which contains a div with a class of spinner
 * @returns A spinner.
 */

const Loader = () => {
  return (
    <div className="loader-section">
      <div className="spinner"></div>
    </div>
  );
};

export default Loader;
